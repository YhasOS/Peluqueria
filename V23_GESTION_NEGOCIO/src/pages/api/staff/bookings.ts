import type { NextApiRequest, NextApiResponse } from 'next';
import prisma from '@/lib/prisma';
const STAFF_COOKIE='gema_staff_auth';
function getStaffId(req:NextApiRequest){const raw=req.cookies?.[STAFF_COOKIE];if(!raw)return null;try{const d=JSON.parse(Buffer.from(raw,'base64url').toString('utf8'));return Number(d.id)||null}catch{return Number(raw)||null}}
export default async function handler(req:NextApiRequest,res:NextApiResponse){try{if(req.method!=='GET')return res.status(405).end();const staffId=getStaffId(req);if(!staffId)return res.status(401).json({error:'No autorizado'});const from=String(req.query.from||new Date().toISOString().slice(0,10));const to=String(req.query.to||from);const start=new Date(`${from}T00:00:00`);const end=new Date(`${to}T23:59:59`);const staff=await prisma.professional.findUnique({where:{id:staffId},select:{id:true,name:true,username:true}});if(!staff)return res.status(401).json({error:'Trabajadora no encontrada'});
const bookings=await prisma.$queryRawUnsafe<any[]>(`SELECT b.id,b."clientName",b."clientPhone",b."clientEmail",b.notes,b."startTime",b."endTime",b."professionalId" as "staffId",p.name as "staffName",COALESCE(string_agg(ms.name, ' + ' ORDER BY bs.position), s.name) as "serviceName",COALESCE(SUM(bs.price), s.price, 0)::float as "servicePrice",COALESCE(SUM(bs.price), s.price, 0)::float as price,COALESCE(b.status,'confirmed') as status FROM "Booking" b LEFT JOIN "Professional" p ON p.id=b."professionalId" LEFT JOIN "Service" s ON s.id=b."serviceId" LEFT JOIN "BookingService" bs ON bs."bookingId"=b.id LEFT JOIN "Service" ms ON ms.id=bs."serviceId" WHERE b."startTime">=$1 AND b."startTime"<=$2 AND COALESCE(b.status,'confirmed')<>'cancelled' GROUP BY b.id,p.name,s.name,s.price ORDER BY b."startTime" ASC`,start,end);return res.status(200).json({staff,bookings});}catch(error:any){console.error('STAFF BOOKINGS ERROR',error);return res.status(500).json({error:'Error cargando agenda',detail:error?.message||String(error)})}}
