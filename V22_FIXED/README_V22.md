# V22 Ticket digital WhatsApp

Desde la raíz del proyecto:

```powershell
git checkout main
git pull
git checkout -b v22-ticket-digital
powershell -ExecutionPolicy Bypass -File "RUTA\V22_TICKET_DIGITAL_WHATSAPP_FIXED\install-v22.ps1"
npm run build
```

Si compila:

```powershell
git add .
git commit -m "V22 ticket digital WhatsApp"
git push -u origin v22-ticket-digital
```
